model Slider {
  id          String   @id @default(uuid())
  heading     String
  subheading  String?
  tagline     String
  image       String
  description String
  category    String
  order       Int      @default(1)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
