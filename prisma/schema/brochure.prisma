model Brochure {
  id          String   @id @default(uuid())
  title       String
  pdfFileUrl  String?  // Made optional with ?
  comingSoon  Boolean  @default(true)  // Fixed syntax
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}