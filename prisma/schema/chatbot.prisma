model Chatbot {
  id        String   @id @default(uuid())
  question  String
  answer    String
    order     Int? 
  createdAt DateTime @default(now())
  updatedAt DateTime @default(now()) @updatedAt
}
