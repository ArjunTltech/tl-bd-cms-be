BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Slider] ADD [order] INT NOT NULL CONSTRAINT [Slider_order_df] DEFAULT 1;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
